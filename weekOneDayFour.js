console.log("The term for creating a variable is declare")

console.log('The term for running a function is to invoke')

const student = {
  Name: "Eva",
  School: "FAU",
  GPA: "4.2"
}

let favoriteFruits = ['mango','lychee','strawberry','papaya']

function modifyArray(arr){
arr.shift()
arr.push('kiwi')
}